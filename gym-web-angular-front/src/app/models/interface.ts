export interface UserI{
    id?: string;
    email: string;
    password: string;
}

export interface CourseI{
    id?: string;
    name: string; 
    email: string;
    password: string;
    courses: string; 
    role: string; 
}